cd "C:/Users/TOURE/Documents/im_workflow/scripts"

# Full workflow including optional reports
Rscript run_workflow.R

# Skip data fetch (if already done) but run everything else including reports
Rscript run_workflow.R --skip-fetch

# Skip reports if not needed
Rscript run_workflow.R --skip-fetch --skip-reports

# Upload only mode
Rscript run_workflow.R --upload-only

# Run only reports (if data already processed)
Rscript run_workflow.R --skip-fetch --skip-build --skip-clean --skip-upload

# Skip everything but reports
Rscript run_workflow.R --skip-fetch --skip-build --skip-clean --skip-upload --skip-reports